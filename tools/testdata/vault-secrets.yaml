# Stub for the hub Vault transit-unseal token that policy-vault-transit-token copies onto spokes via
# fromSecret. In production the transit-provider Job (Component B) creates this in the hub vault ns.
# Low-entropy fake value (not gitleaks-allowlisted here).
apiVersion: v1
kind: Secret
metadata:
  name: vault-transit-unseal-token
  namespace: vault
type: Opaque
data:
  # fake: base64 of "test-token"
  token: dGVzdC10b2tlbg==
