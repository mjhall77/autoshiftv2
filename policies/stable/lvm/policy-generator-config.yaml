apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: lvm
# Layout convention:
#   manifests/ — desired state to APPLY; respects dry-run via remediationAction: ${REMEDIATION}
#   test/      — assertions to OBSERVE (readiness/status/compliance); ALWAYS remediationAction: inform
# A policy's path points at a single FILE, or a DIRECTORY when it has 2+ manifests (a dir is a wildcard:
# drop a file in, no config edit). Keep placement.yaml at the dir root, NOT inside a wildcarded dir (a
# Placement in a wildcarded dir would be wrapped into the Policy as a managed object).
placementBindingDefaults:
  name: binding-policy-lvm
policyDefaults:
  namespace: ${POLICY_NAMESPACE}
  remediationAction: ${REMEDIATION}
  severity: high
  consolidateManifests: true
  evaluationInterval:
    compliant: ${EVAL_COMPLIANT}
    noncompliant: ${EVAL_NONCOMPLIANT}
  standards:
    - NIST SP 800-53
  categories:
    - CM Configuration Management
  controls:
    - CM-2 Baseline Configuration
  # All policies share one hand-authored Placement (predicate autoshift.io/lvm).
  placement:
    placementPath: placement.yaml
policies:
  # 1) Operator install: raw Namespace (PG wraps -> ConfigurationPolicy) + first-class OperatorPolicy.
  #    2 manifests -> a directory (wildcard).
  - name: policy-lvm-operator-install
    manifests:
      - path: manifests/operator-install
  # 2) LVMCluster CR (first-class ConfigurationPolicy). Waits on the operator install.
  - name: policy-lvmcluster
    dependencies:
      - name: policy-lvm-operator-install
    manifests:
      - path: manifests/lvmcluster.yaml
  # 3) LVMCluster readiness gate — a test/ assertion, so ALWAYS inform (a status can only be observed).
  - name: policy-lvmcluster-test
    remediationAction: inform
    dependencies:
      - name: policy-lvmcluster
    manifests:
      - path: test/lvmcluster-ready.yaml
