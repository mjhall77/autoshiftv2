apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: placement-policy-cluster-install
  namespace: ${POLICY_NAMESPACE}
spec:
  # cluster-install runs ON the hub (it creates Hive/SiteConfig resources that provision spoke
  # clusters). No spec.clusterSets: the namespace bindings scope it; cluster-type: hub is the
  # hub-only marker, plus cluster-install: true.
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: 'autoshift.io/cluster-type'
              operator: In
              values:
                - 'hub'
            - key: 'autoshift.io/cluster-install'
              operator: In
              values:
                - 'true'
  tolerations:
    - key: cluster.open-cluster-management.io/unreachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
