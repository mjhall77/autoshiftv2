object-templates-raw: |
  {{- /* vSphere creates one worker MachineSet per failure domain; clone each into an infra MachineSet that copies its workspace/network/template, so infra nodes land in the same failure domain(s) as the workers — works for one or many domains. */ -}}
  {{- $machineset_role := "infra" }}
  {{- $infrastructure_id := (lookup "config.openshift.io/v1" "Infrastructure" "" "cluster").status.infrastructureName }}
  {{- range (lookup "machine.openshift.io/v1beta1" "MachineSet" "openshift-machine-api" "").items }}
    {{- if hasPrefix (printf "%s-worker" $infrastructure_id) .metadata.name }}
      {{- $worker_ms := . }}
      {{- $zone := trimPrefix (printf "%s-worker-" $infrastructure_id) $worker_ms.metadata.name }}
  - complianceType: musthave
    objectDefinition:
      apiVersion: machine.openshift.io/v1beta1
      kind: MachineSet
      metadata:
        labels:
          machine.openshift.io/cluster-api-cluster: {{ $infrastructure_id }}
        name: {{ $infrastructure_id }}-{{ $machineset_role }}-{{ $zone }}
        namespace: openshift-machine-api
      spec:
        deletePolicy: Oldest
        replicas: {{hub index .ManagedClusterLabels "autoshift.io/infra-nodes" hub}}
        selector:
          matchLabels:
            machine.openshift.io/cluster-api-cluster: {{ $infrastructure_id }}
            machine.openshift.io/cluster-api-machineset: {{ $infrastructure_id }}-{{ $machineset_role }}-{{ $zone }}
        template:
          metadata:
            labels:
              machine.openshift.io/cluster-api-cluster: {{ $infrastructure_id }}
              machine.openshift.io/cluster-api-machine-role: {{ $machineset_role }}
              machine.openshift.io/cluster-api-machine-type: {{ $machineset_role }}
              machine.openshift.io/cluster-api-machineset: {{ $infrastructure_id }}-{{ $machineset_role }}-{{ $zone }}
          spec:
            metadata:
              labels:
                node-role.kubernetes.io/{{ $machineset_role }}: ""
            taints:
              - key: node-role.kubernetes.io/{{ $machineset_role }}
                effect: NoSchedule
              - key: node-role.kubernetes.io/{{ $machineset_role }}
                effect: NoExecute
            providerSpec:
              value:
                numCoresPerSocket: {{ {{hub (index .ManagedClusterLabels "autoshift.io/infra-nodes-numcores-per-socket") | default "" hub}} | default $worker_ms.spec.template.spec.providerSpec.value.numCoresPerSocket  }}
                diskGiB: {{ $worker_ms.spec.template.spec.providerSpec.value.diskGiB }}
                snapshot: ''
                userDataSecret:
                  name: {{ $worker_ms.spec.template.spec.providerSpec.value.userDataSecret.name }}
                memoryMiB: {{ {{hub (index .ManagedClusterLabels "autoshift.io/infra-nodes-memory-mib") hub}} | default $worker_ms.spec.template.spec.providerSpec.value.memoryMiB  }}
                credentialsSecret:
                  name: {{ $worker_ms.spec.template.spec.providerSpec.value.credentialsSecret.name }}
                network:
                  devices:
                    - networkName: {{ (index ($worker_ms.spec.template.spec.providerSpec.value.network.devices) 0).networkName }}
                numCPUs: {{ {{hub (index .ManagedClusterLabels "autoshift.io/infra-nodes-numcpu") hub}} | default $worker_ms.spec.template.spec.providerSpec.value.numCPUs }}
                kind: VSphereMachineProviderSpec
                workspace:
                  datacenter: {{ $worker_ms.spec.template.spec.providerSpec.value.workspace.datacenter }}
                  datastore:  {{ $worker_ms.spec.template.spec.providerSpec.value.workspace.datastore }}
                  folder:  {{ $worker_ms.spec.template.spec.providerSpec.value.workspace.folder }}
                  resourcePool: {{ $worker_ms.spec.template.spec.providerSpec.value.workspace.resourcePool }}
                  server: {{ $worker_ms.spec.template.spec.providerSpec.value.workspace.server }}
                template: {{ $worker_ms.spec.template.spec.providerSpec.value.template }}
                apiVersion: machine.openshift.io/v1beta1
    {{- end }}
  {{- end }}
