apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: openshift-image-registry
# Per-deployment scalars (${...}) are substituted by the repo-server CMP (envsubst/sed) BEFORE
# `kustomize build` runs. They live here and in placement.yaml — never in manifests/, whose hub
# templates contain `$vars` that substitution would clobber.
#
# remediationAction: ${REMEDIATION} is set once in policyDefaults (dry-run aware). The OBC-Bound
# readiness check is its own policies[] entry (policy-image-registry-storage-test, remediationAction:
# inform, manifest under test/) — a separate Policy so the enforce root can't clobber the inform check.
placementBindingDefaults:
  name: binding-policy-image-registry
policyDefaults:
  namespace: ${POLICY_NAMESPACE}
  # ${REMEDIATION} = inform when autoshift.dryRun, else enforce.
  remediationAction: ${REMEDIATION}
  severity: high
  consolidateManifests: true
  evaluationInterval:
    compliant: ${EVAL_COMPLIANT}
    noncompliant: ${EVAL_NONCOMPLIANT}
  standards:
    - NIST SP 800-53
  categories:
    - CM Configuration Management
  controls:
    - CM-2 Baseline Configuration
  # Both policies share one hand-authored Placement (predicate autoshift.io/imageregistry).
  placement:
    placementPath: placement.yaml
policies:
  # PVC storage backing. Shares the default placement; the manifest is hub-gated on storage-type.
  - name: policy-image-registry-storage
    consolidateManifests: false
    manifests:
      - path: manifests/image-registry-storage.yaml
  # s3 storage backing, split out because it must wait for ODF. An ObjectBucketClaim created before
  # the NooBaa StorageClass exists is never reconciled — it stays phaseless instead of retrying.
  # Own placement (storage-type: s3) so pvc-backed clusters aren't stranded by the ODF dependency.
  - name: policy-image-registry-storage-s3
    dependencies:
      - name: policy-storage-cluster-test
    placement:
      placementPath: placement-storage-s3.yaml
    manifests:
      - path: manifests/image-registry-storage-s3.yaml
  # Enables the image registry Config; waits on storage to be provisioned.
  - name: policy-image-registry-storage-test
    remediationAction: inform
    manifests:
      - path: test/image-registry-storage-ready.yaml
  - name: policy-enable-image-registry
    dependencies:
      - name: policy-image-registry-storage
    manifests:
      - path: manifests/enable-image-registry.yaml
