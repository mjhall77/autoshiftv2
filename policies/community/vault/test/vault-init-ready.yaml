# Inform-only readiness gate: Compliant once the init Job has succeeded (repo convention — enforce the
# Job in policy-vault-init, observe success here).
apiVersion: batch/v1
kind: Job
metadata:
  name: vault-init-v1
  namespace: vault
status:
  succeeded: 1
