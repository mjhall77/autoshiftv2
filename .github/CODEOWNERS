# CODEOWNERS — required reviewers per path.
#
# IMPORTANT: matching is LAST-RULE-WINS, not additive. For any file, only the last matching
# pattern applies; a more-specific rule REPLACES the owners from earlier rules (including `*`).
# So the global owners must be repeated on every rule to stay required. A pattern with no
# username (e.g. `autoshift/`) REMOVES ownership for that path — never leave a rule ownerless.

# Global owners — required reviewers on all PRs. Kept on every rule below so they always apply.
* @bcarr-rh @johnharmon @FlexibleToast @andymiller96

# Policy charts (global owners + policy owners)
policies/ @bcarr-rh @johnharmon @FlexibleToast @andymiller96 @caseyoc28 @JustinKuli

# Release and tooling (global owners + tooling owners)
Makefile @bcarr-rh @johnharmon @FlexibleToast @andymiller96 @rh-joanders @caseyoc28
scripts/ @bcarr-rh @johnharmon @FlexibleToast @andymiller96 @rh-joanders @caseyoc28
tools/ @bcarr-rh @johnharmon @FlexibleToast @andymiller96 @rh-joanders @caseyoc28

# Documentation (global owners + docs owners)
docs/ @bcarr-rh @johnharmon @FlexibleToast @andymiller96 @rh-joanders @caseyoc28
README.md @bcarr-rh @johnharmon @FlexibleToast @andymiller96 @rh-joanders @caseyoc28

# Note: autoshift/ and .github/workflows/ intentionally have no specific rule, so the global
# `*` owners apply to them. Do not add a bare `autoshift/` line with no owners — that would
# make the directory ownerless.
