# Documentation toolchain, shared by the build check, the publish, and the devcontainer.
#
# A manifest rather than an inline `pip install`, because neither Dependabot nor Renovate reads a
# version out of a `run:` step. Dependabot tracks it through the pip entry in dependabot.yml.
#
# Pinned exactly: Zensical is before 1.0 and ships breaking changes between patch releases.
zensical==0.0.59
