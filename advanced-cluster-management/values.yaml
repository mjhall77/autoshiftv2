# =============================================================================
# Bootstrap values for the initial ACM install (hand-maintained)
# =============================================================================
# The ACM policy is a PolicyGenerator dir with no values.yaml, so this bootstrap chart is NOT
# auto-generated. Keep these values aligned with the PG manifests when bumping channels/versions.
# =============================================================================

# Bootstrap-specific settings
ignoreHelmHooks: false

# Job image for waiting for CRD
# Use internal registry for disconnected environments
image: image-registry.openshift-image-registry.svc:5000/openshift/cli:latest

acm:
  namespace: open-cluster-management
  channel: release-2.17
  sourceNamespace: openshift-marketplace
  source: redhat-operators
  name: advanced-cluster-management
  disable_hub_self_management: false
  availabilityConfig: High
  enable_backup: false
  gitops_namespace: openshift-gitops
  provisioning:
    # storage_class:
    database_size: 10Gi
    filesystem_storage_size: 100Gi
    image_storage_size: 100Gi
    # hubClusterSets:
    #   hub:
    #     labels:
    #       test1: 'test'
    # managedClusterSets:
    #   sandbox:
    #     labels:
    #       test3: 'test'
    #   managed:
    #     labels:
    #       test4: 'test'
  installPlanApproval: Automatic
