*.swp
.vscode
*.swo
.DS_Store
# CLAUDE.md and AGENTS.md are committed. Personal instructions go in CLAUDE.local.md,
# which Claude Code loads automatically alongside CLAUDE.md.
CLAUDE.local.md
pull-secret.txt
pull-secret.json
imageset*.yaml
.claude
.claude/*
autoshift/files/
.helm-charts/
release-artifacts/
.cache/
.tools/
.tmp/
.mcp.json
.tmp
.tmp/*
local/
memory

# kustomize --enable-helm pulls remote charts into a local charts/ cache next to the
# kustomization (e.g. a direct `kustomize build` of a policy using helmCharts with repo:).
# The e2e/release renders use temp dirs; this only appears on a direct in-tree render.
policies/**/charts/

# Vale styles are downloaded by `vale sync` from the package declared in .vale.ini — a build
# dependency, not source. The vocabulary under .vale/styles/config/ IS ours and is tracked.
.vale/styles/*
!.vale/styles/config/

# Zensical build output. Written next to the config because docs_dir is the repo root.
/site/

# Zensical build cache.
.zensical/
